#!/bin/bash

curl --silent --location --fail https://github.com/quarto-dev/quarto-cli/releases/download/latest/quarto-linux-amd64.deb > /tmp/quarto.deb
sudo apt-get -qq install --yes --no-install-recommends /tmp/quarto.deb